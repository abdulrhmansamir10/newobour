Circular avatar with image or initials fallback; optional gold verification ring.

```jsx
<Avatar name="أحمد سمير" size={44} />
<Avatar src="/u/12.jpg" name="مكتب الصواري" ring />
```

Props: `src`, `name` (derives initials), `size` (px), `ring` (gold).
