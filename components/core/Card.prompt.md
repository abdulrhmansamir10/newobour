Base surface container for grouped content — news items, dashboard panels, forms.

```jsx
<Card padding="md">…</Card>
<Card interactive onClick={open}>…</Card>
```

Props: `padding` (`none | sm | md | lg`), `interactive` (hover lift), `as`. White surface, 16px radius, subtle navy-tinted shadow.
